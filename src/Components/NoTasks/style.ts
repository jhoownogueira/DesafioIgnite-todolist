import styled from "styled-components";

export const NoTasksList = styled.section`
    width: 100%;
    height: 244px;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 64px 0;
    border-radius: 8px;
    border-top: 1px solid var(--secondary);
    img {
        opacity: 0.7;
    }
    p {
        margin-top: 16px;
        width: 100%;
        color: var(--text-light);
        text-align: center;
    }

`